package com.example.ratest.data.repository
//import com.google.ar.core.Anchor
//import com.google.ar.core.Session
//import javax.inject.Inject

//class EarthRepository @Inject constructor(
//    private val session: Session
//) {
//    fun createAnchor(latitude: Double, longitude: Double, altitude: Double): Anchor {
//        val pose = floatArrayOf(latitude.toFloat(), longitude.toFloat(), altitude.toFloat(), 0f, 0f, 0f, 1f)
//        return session.createAnchor(pose)
//    }
//}
